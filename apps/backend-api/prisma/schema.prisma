generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Define models (e.g., User, Course, Lesson) following the domain entities and migrations plan.
